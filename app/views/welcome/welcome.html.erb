<% if session[:user_id] %>
<h1> Hi! You are:</h1>
<h1>User #<%= session[:user_id] %> </h1>
<% @user = User.find_by_id(session[:user_id])%>
<% else %>
<br>
  <h1> Hi! If you're new, use the sign in link in the sidebar! </h1>
    <h1>Otherwise, feel free to sign in with Google!</h1>

  <%= link_to('Log in with Google!', '/auth/google_oauth2') %>

<% end %>
